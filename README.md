### Brock University Library Resource Lookup

This tool has been built to make library resource and library personnel search easy.

To deploy locally, just clone the repository and run -
```./gradlew clean build```
```./gradlew run```

#### System Requirements

Java 21 or above needs to be installed, and JAVA_HOME needs to added to the file path.
